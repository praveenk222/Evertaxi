// Assuming you have a 'User' model

class PostResult {
    constructor(id, status, message) {
      this.id = id;
      this.status = status;
      this.message = message;
    }
  
    // Additional methods can be added, e.g., save to database, update, delete, etc.
  }
  
  // Example usage
//   const user1 = new User(1, 'john_doe', 'john@example.com');
  
//   const user2 = new User(2, 'jane_doe', 'jane@example.com');
  